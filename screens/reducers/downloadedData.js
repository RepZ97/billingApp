
const currentState = {
    dataList: [ {name: 'Face Mask', key: 1},
                {name: 'Water Bottle', key: 2}]
}

const downloadedData = (state = currentState) => {
    return state;
}


export default downloadedData;

